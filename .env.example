# Signaling server URL used by the browser client.
# VITE_ variables are inlined into the client build and are not secrets.
VITE_SIGNALING_URL=http://localhost:3001

# Origin the signaling server accepts browser connections from.
CLIENT_ORIGIN=http://localhost:5173

# Port the signaling server listens on.
PORT=3001
